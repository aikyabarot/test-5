import type { PageProps } from '../types';

const ClientDetailPage: React.FC<PageProps> = () => {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-4">Client Details</h1>
      <p>Client details will go here.</p>
    </div>
  );
};

export default ClientDetailPage;